env:
  mode: dev
logger:
  level: info
  development: true
  outputPaths: # log output
    - log/chop.log
  errorOutputPaths: # only for zap internal error logs
    - log/zap.log
mediaServer:
  listenPort: 8080
cache:
  cache_ttl_sec: 60
  clear_interval_sec: 60
  verbose: false
