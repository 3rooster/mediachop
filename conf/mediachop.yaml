env:
  mode: dev
logger:
  level: info
  development: true
  outputPaths: # log output
    - log/chop.log
  errorOutputPaths: # only for zap internal error logs
    - log/zap.log
mediaServer:
  listenPort: 8080
cache:
  entry_ttl_sec: 10
  clear_interval_sec: 20
  verbose: false
